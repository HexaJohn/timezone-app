// src/features/currentTime/index.ts

// Export the CurrentTime component for use in other parts of the application
export { CurrentTime } from "./components/CurrentTime";

// Export the useCurrentTime hook for use in other parts of the application
export { useCurrentTime } from "./hooks/useCurrentTime";

// Export the types for use in other parts of the application
export type { CurrentTimeProps, CurrentTimeHook } from "./types";
