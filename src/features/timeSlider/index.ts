// src/features/timeSlider/index.ts

// Export the TimeSlider component for use in other parts of the application
export { TimeSlider } from "./components/TimeSlider";

// Export the useTimeSlider hook for use in other parts of the application
export { useTimeSlider } from "./hooks/useTimeSlider";

// Export the types for use in other parts of the application
export type { TimeSliderProps, TimeSliderHook } from "./types";
